version: "3.9"

services:
  gateway:
    build: ..
    ports:
      - "3001:3001"

  redis:
    image: redis:7

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3001:3001"
